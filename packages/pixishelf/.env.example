# PixiShelf local Next.js development environment
# Copy this file to .env.local when running packages/pixishelf directly with pnpm.

# Database running on the host machine or exposed from Docker Compose.
POSTGRES_USER=pixishelf
POSTGRES_PASSWORD=password
POSTGRES_DB=pixishelf
DATABASE_URL=postgresql://pixishelf:password@127.0.0.1:5432/pixishelf?connection_limit=20&pool_timeout=20

# Auth and admin bootstrap.
JWT_SECRET=your-very-secure-jwt-secret-key-here-at-least-32-characters
JWT_TTL=604800
BETTER_AUTH_SECRET=your-very-secure-jwt-secret-key-here-at-least-32-characters
BETTER_AUTH_URL=http://localhost:5430
BETTER_AUTH_TRUSTED_ORIGINS=http://localhost:5430

INIT_ADMIN_USERNAME=admin
INIT_ADMIN_PASSWORD=admin123
ADMIN_INIT_ENABLED=true
ADMIN_INIT_FORCE_RESET=false

# Local filesystem path used when Next.js runs outside Docker.
SCAN_PATH=D:\your\pixiv\data
# URL archive and media jobs use the same host filesystem root in F5 development.
ARCHIVE_STORAGE_PATH=D:\your\pixiv\data
# Optional override for generated posters, chapter previews, and future derived media.
# The default is <project>/.local-data/derived-media and matches docker-compose.dev.yml.
# DERIVED_MEDIA_STORAGE_PATH=D:\your\PixiShelf\packages\pixishelf\.local-data\derived-media

# Central Dispatcher safety switches. Keep these values paired: true/true for
# production, or false/false for dark start and incident isolation. The legacy
# archive worker cannot run on the dual-lane schema.
CENTRAL_DISPATCHER_CUTOVER_ENABLED=true
WORKER_DISPATCH_ENABLED=true

# Local image service exposed by Docker Compose.
NEXT_PUBLIC_IMGPROXY_URL=http://127.0.0.1:5431

# Optional outbound proxy for URL archives. ARCHIVE_HTTPS_PROXY overrides the
# standard HTTPS_PROXY/HTTP_PROXY variables; set it to an empty value to force
# direct connections. Clash Verge commonly listens on 127.0.0.1:7890.
# ARCHIVE_HTTPS_PROXY=http://127.0.0.1:7890

# Internal and webhook tokens for local API testing.
INTERNAL_JOB_TOKEN=replace-with-a-long-random-token
SCAN_WEBHOOK_TOKEN=replace-with-a-long-random-token
