import { redirect } from 'next/navigation'

// ============================================================================
// 根页面 - 重定向到仪表板
// ============================================================================

/**
 * 根页面组件
 * 根据 Next.js 15 最佳实践，使用 redirect 函数进行服务端重定向
 */
export default function RootPage() {
  // 使用 Next.js 15 的 redirect 函数进行服务端重定向
  // 这是推荐的做法，因为它在服务端执行，避免了客户端闪烁
  redirect('/dashboard')
}