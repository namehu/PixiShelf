{
  "name": "pixishelf",
  "version": "1.0.0",
  "description": "A modern personal web gallery for local image collections",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm --r build",
    "format": "prettier --write ."
  },
  "keywords": [
    "gallery",
    "images",
    "monorepo",
    "react",
    "fastify"
  ],
  "author": "PixiShelf Team",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.8.0",
    "lint-staged": "^15.0.2",
    "oxlint": "^1.32.0",
    "prettier": "^3.0.3",
    "shadcn": "^3.3.1"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "pnpm": {
    "overrides": {
      "@wxt-dev/browser": "^0.1.4"
    }
  }
}